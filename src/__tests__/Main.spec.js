// import { Main } from '../containers/Main';

describe('<Main />', () => {
  // let wrapper;

  // beforeEach(() => {
  //   wrapper = shallow(<Main />);
  // });

  it('renders without crashing', null);
});
